<h1>Property Managers</h1>

<table class='table'>
  <tr>
    <th>Name</th>
    <th>Buildings Owned</th>
  </tr>
  <% @property_managers.each do |manager| %>
    <tr>
      <td><%= link_to manager.name, property_manager_path(manager) %></td>
      <td><%= manager.buildings.size %></td>
    </tr>
  <% end %>
</table>
